https://codepen.io/remyvila/pen/abNrJbe?editors=1100    

    1. What is Semantic HTML? tags that do not effect the code other than for clarity for the viewer and for SEO.
    2. What is HTML used for? Writing the base elements of a web page
    3. What is an attribute and where do we put it? A modifier of a tag, and we put in on the inside of the <>'s of the opening tag.
    4. What is the h1 tag used for? How many times should I use it on a page? The largest font size header. Once.
    5. Name two tags that have required attributes. <img> and <a>
    6. What do we put in the head of our HTML document? The header...? Title?
    7. What is an id? something for calling to a single part in a piece of code, by typing #idname
    8. What elements can I add an id to? Almost any element. Only not <br> and even then maybe <br> too.
    9. How many times can I use the same id on a page?  Once.
    10. What is a class? Like an id but make be used multiple times and when it is called on later modifies every instace of its' usage.
    11. What elements can I add a class to? Same as id.
    12. How many times can I use the same class on a page? As many as you like.
    13. How do I get my link to open in a new tab? Using <a _blank>
    14. What is the alt attribute in the image tag used for? For blind people and almighty google to know what the picture is of.
    15. How do I reference an id? with #
    16. What is the difference between a section and a div. Sections are for SEO and div is a block level element.
    17. What is CSS used for? Stylizing web pages.
    18. How to we select an element? Example - every h2 on the page h2{}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. Class can be used infinitely and id only once. id for something
you only need to have a single set of conditions modify a single portion of your code. Class where you may want to have a single call with conditions modify multiple parts.
    20. How do we select classes in CSS? with a .classname
    21. How do we select a p element with a single class of “human””? .human p{}
    22. What is a parent child selector? When would this be useful? A tool for selecting both a parent and a child, useful for when you want a single
condition to apply to both.
    23. How do you select all links within a div with the class of sidebar? href .sidebar{} maybe?
    24. What is a pseudo selector? a selector for modifying classes. How I might think of it as CSS's simplified version of an if statement.
    25. What do we use the change the spacing between lines? line-height:unitOfMeasure;

    26. What do we use to change the spacing between letters? letter-spacing:
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? uppercase, lowercase and capitalize
    28. How do I add a 1px border around my div that is dotted and black?
 .class{
border-style:dotted;
border-width:1px;
border-color:black;
    29. How do I select everything on the page? select a tag the engulfs the entire page.
    30. How do I write a comment in CSS? /* comment */
    31. How do I find out what file I am in, when I am using the command line? $ git status
    32. Using the command line - how do I see a list of files/folders in my current folder? $ ls
    33. How do I remove a file via the command line? Why do I have to be careful with this?  rm filename. Because there is no clarification it just does it when
you tell it to.
    34. Why should I use version control? To more efficiently use groups of coders and make branching pathes while keeping record of effective code you have but may
want to attempt to grow or differ from.
    35. How often should I commit to github? When you make changes to your repo.
    36. What is the command we would use to push our repo up to github? $ git commit -m "debriefing of changes made"
    37. Walk me through Lambda's git flow. I don't understand the question.